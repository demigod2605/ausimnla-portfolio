-- Run this in the Supabase SQL editor to remove all current project rows.
-- When you're ready to add real projects later, insert new rows the same
-- way (see supabase/schema.sql or supabase/add_more_projects.sql for the
-- insert format), or add them directly via Table Editor.

delete from public.projects;
